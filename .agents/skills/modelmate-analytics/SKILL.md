---
name: modelmate-analytics
description: Use when implementing history, SQLite storage, frequent questions, categories, or app skills that reuse prior answers.
---

Keep analytics version-scoped. Normalize questions before hashing. Classify with deterministic rules first. FAQ/history reuse must not cross versions. Preserve JSONL as raw append-only audit while SQLite powers query and dashboards.
